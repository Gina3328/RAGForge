# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets
.env

# OS / IDE
.DS_Store
.idea/

# Course/book reference screenshots -- personal planning material, not
# meant to be part of the public repo (see README note on this project's
# independent authorship)
docs/

# Raw evaluation output -- regenerated by evaluation/*.py against Milvus/
# an LLM judge, and full of superseded intermediate re-runs from ongoing
# debugging (not a clean, presentable result set). Curated final results
# get added deliberately, separately, when ready to present.
evaluation/reports/

# Evaluation source documents -- deferred decision on whether to publish
# these (mix of third-party PDFs and one authored file); revisit later.
evaluation/documents/
